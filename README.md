# Amazon Best Sellers Data Scraper

**基于 Python 的亚马逊畅销榜数据分析工具**

本项目是一个用于抓取亚马逊（Amazon）特定品类畅销榜（Best Sellers）数据并分析商品详情的 Python 脚本。它专为跨境电商市场分析设计，能够快速获取榜单 ASIN 并多线程抓取商品核心数据（价格、评分、评论数等），最终生成 Excel 报表。

---

## 核心功能

- **榜单 ASIN 提取**：自动解析亚马逊 Best Sellers 页面，提取 Top 50 商品 ASIN。
- **多线程并发抓取**：采用 `ThreadPoolExecutor` 实现多线程并发，大幅提升数据抓取速度（相比串行抓取提升 5-10 倍）。
- **反爬虫对抗**：
    - 内置动态 User-Agent 池。
    - 支持本地代理（如 Shadowrocket/Clash），防止 IP 被封。
    - 智能重试机制与随机延时策略。
- **数据清洗与导出**：自动清洗价格、评分等字段，最终生成结构化的 Excel 文件。

---

## 环境依赖

请确保已安装 Python 3.x，并安装以下依赖库：

```bash
pip install requests beautifulsoup4 pandas fake-useragent openpyxl
```

---

## 快速开始

### 1. 配置代理（重要）

亚马逊对爬虫限制严格，建议开启本地代理工具（如小火箭、Clash）。

1. 打开代理软件设置。
2. 开启 **“允许来自局域网的连接”**。
3. 记下端口号（通常为 `1082` 或 `7890`）。

### 2. 修改配置

在代码的 `配置区域` 中修改以下内容：

- `SAVE_DIR`：你的文件保存路径。
- `TARGET_URL`：你想抓取的亚马逊榜单链接。
- `PROXIES`：填入你的本地代理地址。

### 3. 运行脚本

```bash
python get_data.py
```

运行成功后，你将在指定目录下看到生成的 `iphone_cases_top50_fast.xlsx` 文件。

---

## 常见问题

### 获取失败，状态码：503

- **原因**：IP 被亚马逊暂时封禁。
- **解决**：检查代理是否开启，或更换代理节点。

### 发现 0 个带有 data-asin 的元素

- **原因**：亚马逊页面结构更新或反爬拦截。
- **解决**：程序会自动保存 `debug_amazon.html`，请打开该文件检查是否为验证码页面。

### 程序运行报错 `NameError: name 'random' is not defined`

- **解决**：请确保代码顶部已包含 `import random`。

---

## 免责声明

本项目仅供学习和技术交流使用。请勿用于商业用途或高频恶意抓取。抓取数据请遵守亚马逊的 `robots.txt` 协议及相关法律法规。

---

## 更新日志

- **v1.1**：引入多线程抓取，优化速度；修复 ASIN 解析逻辑。
- **v1.0**：初始版本，实现基础串行抓取。

这三张图表分别揭示了iPhone 17手机壳市场的价格分布、品牌竞争格局以及头部品牌的价格策略。

### 价格分布分析：低价走量是主流策略

- **数据解读**：第一张图（价格分布直方图）显示，绝大多数手机壳的价格集中在 **5 SGD 到 20 SGD** 之间。峰值出现在 **10 SGD - 12 SGD** 左右，随后随着价格升高，产品数量急剧下降。在 30 SGD 以上的高价位区间，产品数量非常稀少。
- **商业洞察**：
  - **价格敏感度高**：市场呈现明显的“长尾分布”，说明大部分消费者对于手机壳这类配件的价格较为敏感，倾向于购买平价产品。
  - **低价竞争红海**：5-15 SGD 区间竞争最为激烈，是流量的主要入口。
  - **高端市场小众**：超过 30 SGD 的产品属于小众市场，可能针对的是具有特殊功能（如防摔认证、特殊材质）的高端用户。

### 品牌竞争格局：市场高度分散，头部效应微弱

- **数据解读**：第二张图（品牌数量柱状图）显示，排名前两位的品牌（Miracase 和 FNTCASE）各自仅有 2 款相关产品，而随后的 8 个品牌（SUPFINE, Spigen, OtterBox 等）均只有 1 款产品。
- **商业洞察**：
  - **极度碎片化**：Top 10 品牌之间的产品数量差异极小（2 vs 1），说明目前 iPhone 17 手机壳市场没有绝对的垄断者，处于“百家争鸣”的早期阶段。
  - **准入门槛低**：即使是知名品牌（如 OtterBox, Spigen）也仅有一款产品上榜，意味着新品牌进入该市场的门槛较低，机会均等。
  - **店铺命名特征**：品牌名称多带有 "Store" 后缀（如 "Visit the Miracase Store"），这暗示数据可能主要来源于亚马逊等电商平台，且卖家倾向于通过店铺品牌化来运营。

### 头部品牌价格策略：品牌溢价与定位分层明显

- **数据解读**：第三张图（箱线图）展示了 Top 5 品牌的价格差异：
  - **低价区**：SUPFINE 价格极低且稳定（约 5-6 SGD）。
  - **中低价区**：FNTCASE 价格集中在 5-10 SGD，且有少量高价 outlier。
  - **中价区**：Miracase 价格集中在 16-19 SGD，明显高于前两者。
  - **高价区**：Spigen 和 OtterBox 价格极高。Spigen 稳定在 19 SGD 左右，而 OtterBox 价格极高且极其稳定（约 45 SGD，箱体呈一条线）。
- **商业洞察**：
  - **明确的梯队划分**：
    - **性价比梯队**：SUPFINE, FNTCASE（主打便宜走量）。
    - **中端设计梯队**：Miracase（价格适中，可能主打设计或轻薄）。
    - **高端功能梯队**：Spigen, OtterBox（主打强防护，拥有极高的品牌溢价，尤其是 OtterBox，价格约为低价品牌的 9 倍）。
  - **定价策略差异**：OtterBox 和 SUPFINE 的箱体非常扁平（甚至是一条线），说明这两个品牌采取了**单一爆款策略**或**统一定价策略**，不在此品类中做价格区分；而 FNTCASE 和 Miracase 的箱体较长，说明它们在同一品类下尝试了不同的定价组合。

### 总结与建议

综合来看，iPhone 17 手机壳市场目前是一个**“低价为主、品牌分散、定位分层”**的市场。

- **对于新进入者**：如果打价格战，很难低于 SUPFINE (5 SGD)；如果做高端，OtterBox (45 SGD) 的护城河很深。
- **机会点**：15 SGD - 25 SGD 的中端市场（Miracase 所在区间）竞争相对不那么拥挤，且高于底价区，可能是一个兼顾利润与销量的“甜蜜点”。


